q0 q1 qf #estados ex: q0 q1 q2

a b ? # alfabeto ex: a b

A B ε # simbolos pilha ex:: A B C

δ : #Digite por linha(estadoatual, fita, pilha, desenpilha, estado destino)
q0 a ε B q0
q0 b B ε q1
q0 ? ? ε qf
q1 b ε ε q0
q1 ? ? B qf

q0 #estado inicial

qf #estado final